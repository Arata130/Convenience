@using Convenience.Models.ViewModels.Shiire;
@model ShiireKeyViewModel;

@{
    ViewData["Title"] = "ShiireKey";
}

<h3>仕入</h3>

<form asp-action="ShiireKey" method="post">
    <div class="form-group">
        <label asp-for="ChumonId" class="control-label"></label>
        <select asp-for="ChumonId" asp-items="@Model.ChumonIdList" class="form-control" name="inChumonId">
            <option value="">-- 選択してください --</option>
        </select>
        <span asp-validation-for="ChumonId" class="text-danger"></span>
    </div>
    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>
